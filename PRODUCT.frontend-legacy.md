# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要面向需要从指定知识库中检索信息并获得可核查回答的中文用户。用户在登录后选择知识库、创建或继续对话、提出问题，并通过来源与检索分数核验回答依据。

## Product Purpose

提供一个中文可视化 RAG 助手，让用户从选定知识库获得流式回答，同时看见检索、融合、重排序和证据判断过程。成功意味着用户不仅得到答案，也能理解答案来自哪里，以及现有证据是否足以支持回答。

## Positioning

产品将 RAG 的证据链直接呈现给用户：同时展示向量检索、BM25、RRF 融合与重排序阶段，标明证据是否充足，并提供可展开的来源内容和各项检索分数，而不是只输出不可核查的聊天答案。

## Operating Context

- 用户通过账号注册和登录进入应用。
- 用户从后端提供的知识库列表中选择可用知识库，并查看其状态、文档数和片段数。
- 用户创建新对话或继续历史对话，问题和回答按会话保存。
- 回答通过 SSE 流式生成；界面同步呈现检索阶段、来源、证据判断和错误状态。
- 用户可以打开来源详情，查看知识库、源文件、原始片段位置及检索分数。

## Capabilities and Constraints

- 保留登录、注册、登录拦截、知识库选择、新建与删除对话、历史记录、流式问答、Markdown 回答、来源详情和检索评分能力。
- 检索术语包括向量检索、BM25、RRF 融合、重排序、来源和证据充足性判断。
- 前端使用 Vue 2、Vue Router、Element UI、Axios、Marked、DOMPurify 和 Webpack。
- 当前后端基地址为 `http://localhost:8001/`；前端依赖现有 users、chat 和 history 接口以及 SSE 事件契约。
- 主要界面语言为简体中文。
- 不虚构用户规模、准确率、性能基准、客户、推荐语、生产部署状态或其他未经证实的产品证明。

## Brand Commitments

产品名称为“可视化 RAG 助手”。界面采用直接、技术可信、以证据为中心的中文表达；现有署名为“Made by LBA”。

## Evidence on Hand

- 当前实现能够展示知识库状态、文档数、片段数、向量与 BM25 命中数、融合候选数、来源数及证据判断。
- 当前实现能够展示向量相似度或距离、BM25、RRF 和重排序分数。
- 当前仓库没有已确认的用户数据、性能基准、客户案例、推荐语或生产部署证明，未来工作不得自行补造。

## Product Principles

1. 回答必须可追溯到明确来源，而不是只呈现生成结果。
2. 将复杂的 RAG 过程转化为用户可理解的阶段和证据状态。
3. 明确区分证据充足、证据不足、处理中断和系统错误。
4. 保持知识库、会话、回答和来源之间的上下文一致。
5. 优先保证真实、可核查的产品表达，不以虚构证明包装能力。
